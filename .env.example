# JWT 密钥，可以执行 `openssl rand -hex 32` 生成
SECRET_KEY=your_super_secret_key_here

# PostgreSQL 数据库连接地址
# 格式: postgresql://[user]:[password]@[host]:[port]/[db_name]
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/todo_list
